export const API_PATH_SEND_QUESTION = "/api/question";
export const API_PATH_REGISTER = "/api/register";
export const API_PATH_LOGIN = "/api/login";

export const API_PATH_BEST_REVIEWS = "api/bestReviews";


// Frontend routes
export const PATHS = {
  home: '/home',
  login: '/login',
  register: '/register',
}

// Backend routes
const API_PATH = '/api/v1'

export const API_PATHS = {
  bestReviews: `${API_PATH}/bestReviews`,
  sendQuestion: `${API_PATH}/question`,
  register: `${API_PATH}/createuser`,
  login: `${API_PATH}/login`
}